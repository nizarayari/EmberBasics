<h2>My favorite Orgs : </h2>

<ul>
  {{#each content as |org|}}
    <li>
      <span {{action 'addToFavorites' org}}> [FAVORITE] </span>
      {{link-to org.id 'org' org.id}}
      }
    </li>
  {{/each}}
</ul>

{{outlet}}
